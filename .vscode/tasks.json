{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "grunt",
        "command": "grunt",        
        "type": "shell",
        "options": {
            "cwd": "${workspaceFolder}"
          }
      },
      {
        "label": "clean",
        "command": "grunt",        
        "type": "shell",
        "args": ["runClean"],
        "options": {
            "cwd": "${workspaceFolder}"
          }
      }
    ]
  }